version: '3.4'
services:
  web:
    build: 
      context: .
      dockerfile: Dockerfile
    environment:
      - ASPNETCORE_ENVIRONMENT=Production
    ports:
      - "80:80"
    depends_on:
      - db
  db:
    image: mongo:latest
    volumes:
      - pitagorassns_data:/data/db